spring:
  application:
    name: user-service

  data:
    mongodb:
      uri: mongodb+srv://user1:IOCEZCXnmlB9YrvZ@cluster0.itsriak.mongodb.net/dms-user-db?retryWrites=true&w=majority

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

server:
  port: 8082
